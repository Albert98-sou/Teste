name: RDP Windows 10 Gamer (Intel Xeon)

on:
  workflow_dispatch:

# ====== CONFIGURAÇÃO DO PC (XEON / DATACENTER) ======
env:
  PC_STORAGE: "1TB NVMe (Cloud)"
  PC_RAM: "16GB / 32GB DDR4 ECC"
  PC_CPU: "Intel Xeon (Datacenter Class)"
  PC_GPU: "NVIDIA Tesla T4 / RTX (Virtual)"
  PC_OS: "Windows 10 Pro 64-bit"

jobs:
  xeon-rdp:
    runs-on: windows-2022
    timeout-minutes: 360

    steps:
      - name: Mostrar Configuração do PC
        shell: powershell
        run: |
          Write-Host "================================="
          Write-Host "   CLOUD PC - INTEL XEON"
          Write-Host "================================="
          Write-Host "CPU : $env:PC_CPU"
          Write-Host "RAM : $env:PC_RAM"
          Write-Host "GPU : $env:PC_GPU"
          Write-Host "SSD : $env:PC_STORAGE"
          Write-Host "OS  : $env:PC_OS"
          Write-Host "================================="

      - name: Ativar RDP (Sem NLA)
        shell: powershell
        run: |
          Set-ItemProperty `
            -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" `
            -Name "fDenyTSConnections" -Value 0

          Set-ItemProperty `
            -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" `
            -Name "UserAuthentication" -Value 0

          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Restart-Service TermService -Force

      - name: Criar Usuário RDP (Admin)
        shell: powershell
        run: |
          $user = "RDP"
          $pass = ([char[]](48..57 + 65..90 + 97..122) | Get-Random -Count 16) -join ""
          $secure = ConvertTo-SecureString $pass -AsPlainText -Force

          New-LocalUser $user -Password $secure -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member $user
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member $user

          echo "RDP_USER=$user" >> $env:GITHUB_ENV
          echo "RDP_PASS=$pass" >> $env:GITHUB_ENV

      - name: Instalar Tailscale
        shell: powershell
        run: |
          Invoke-WebRequest `
            https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi `
            -OutFile tailscale.msi

          Start-Process msiexec.exe `
            -ArgumentList "/i tailscale.msi /quiet /norestart" -Wait

      - name: Conectar ao Tailscale
        shell: powershell
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up `
            --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} `
            --hostname=xeon-win-${{ github.run_id }}

          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "TAILSCALE_IP=$ip" >> $env:GITHUB_ENV

      - name: Dados de Acesso RDP
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "========= RDP XEON ========="
          Write-Host "IP      : $env:TAILSCALE_IP"
          Write-Host "Usuário : $env:RDP_USER"
          Write-Host "Senha  : $env:RDP_PASS"
          Write-Host "============================"
          Write-Host ""

      - name: Manter PC Ligado
        shell: powershell
        run: |
          while ($true) {
            Write-Host "[$(Get-Date)] Windows Xeon ativo..."
            Start-Sleep -Seconds 300
          }
